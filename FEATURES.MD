How can we prevent that the user doesn't draw outside the paper?
Lines are not working when drawing to the top or left.
Shapes floating bar positioning in the middle
Polygon alpha doesn't do anything (or it appears so)
It allows adding links with no target so when I export I get an error.
Polygon we need to tell the user how it works (at least for the first time)
How can i set the page size?